/*
Provider blocks are intentionally deferred in the hybrid route.

This blueprint can be promoted into a project targeting vSphere, OpenShift,
bare metal Kubernetes, or another on-prem platform, so provider selection
belongs in that derived implementation rather than the shared scaffold.
*/
