# Hybrid Architecture Workflow

![Hybrid workflow](assets/diagrams/workflow.svg)

The diagram shows the self-hosted data path and the integrated control path so it is clear how replay, deduplication, and delete recovery are handled without relying on managed cloud defaults.
